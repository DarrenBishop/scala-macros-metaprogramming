# Obective

## Add type-safety to (string-dominated) JDBC queries

## A macro that's "too powerful for its own good"
 - actions at compile-time
 - mapping types
 - adding extra type-safety
 - generating correct types
